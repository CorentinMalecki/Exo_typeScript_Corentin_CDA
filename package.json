{
  "name": "project",
  "description": "Projet TypeScript",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "preview": "node dist/index.js",
    "clean": "rmdir /s /q dist 2>nul || true",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.0.0"
  }
}